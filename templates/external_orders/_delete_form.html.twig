<form method="post" action="{{ path('external_orders_delete', {'id': external_order.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ external_order.id) }}">
    <button class="btn">Delete</button>
</form>
