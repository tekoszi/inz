{% extends 'base.html.twig' %}

{% block title %}Warehouses{% endblock %}

{% block body %}
    <h1>Warehouses</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ warehouse.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ warehouse.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('warehouses_index') }}">back to list</a>

    <a href="{{ path('warehouses_edit', {'id': warehouse.id}) }}">edit</a>

    {{ include('warehouses/_delete_form.html.twig') }}
{% endblock %}
