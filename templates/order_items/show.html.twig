{% extends 'base.html.twig' %}

{% block title %}OrderItems{% endblock %}

{% block body %}
    <h1>OrderItems</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order_item.id }}</td>
            </tr>
            <tr>
                <th>Order_id</th>
                <td>{{ order_item.orderId }}</td>
            </tr>
            <tr>
                <th>Product_id</th>
                <td>{{ order_item.productId }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ order_item.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('order_items_index') }}">back to list</a>

    <a href="{{ path('order_items_edit', {'id': order_item.id}) }}">edit</a>

    {{ include('order_items/_delete_form.html.twig') }}
{% endblock %}
